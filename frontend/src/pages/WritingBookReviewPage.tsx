import * as React from 'react';
import styled from 'styled-components';

const PageLayout = styled.div`
  margin-top: 70px;
  padding: 0 300px;
`;

const WritingBookReviewPage: React.SFC<{}> = () => <PageLayout>dd</PageLayout>;

export default WritingBookReviewPage;
